# Blackjack Package Placeholder

This directory is reserved for the first real game module.

When implementation starts, add a Composer package here that implements
`Amu\Core\Contracts\GameModule` and registers itself with the core module registry.
